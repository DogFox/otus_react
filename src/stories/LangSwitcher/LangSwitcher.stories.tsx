import type { Meta } from '@storybook/react';
import { LangSwitcherDemo } from './LangSwitcherDemo';

const meta: Meta<typeof LangSwitcherDemo> = {
  title: 'Example/LangSwitcher',
  component: LangSwitcherDemo,
  tags: ['autodocs'],
};

export default meta;

export const Default = {};
